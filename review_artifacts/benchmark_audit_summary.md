# Review Artifact Summary

This folder was generated by `python generate_review_artifacts.py`.

## What It Proves

- `inference_stdout_fallback_sample.txt` shows the strict `[START]`, `[STEP]`, `[END]` stdout contract with no extra lines.
- `hard_cautious_transcript.json` captures the full cautious hard-task run used in the benchmark comparison.
- `expert_cautious_transcript.json` captures the full cautious expert-task run used to verify phantom-alert handling.
- `recovery_cautious_transcript.json` captures the full cautious recovery-task run used to verify transient degradation handling.
- `hard_aggressive_transcript.json` captures a hard-task aggressive rollout example that promotes through the watch window and rolls back late.
- `expert_phantom_hold_grader.json` isolates the expert phantom-alert hold decision that should score well.
- `recovery_transient_hold_grader.json` isolates the recovery hold decision that should score well without rollback.
- `hard_watch_window_hold_grader.json` isolates the watch-window hold decision that makes the hard task meaningfully discriminative.
- `policy_benchmark_results.json` compares shallow, cautious, and aggressive policies across easy, medium, hard, expert, recovery, and silent.
- `policy_seed_sweep_results.json` shows the same policy ordering across deterministic seeds 0, 3, 5, 7, 11, 13, 19, 23.
- `scenario_variant_catalog.json` exposes how warning, noise, phantom, recovery-clear, and breach timing vary across seeds for every public task family.
- `openenv_validation_results.json` records both structural and live OpenEnv validator results from the same local run.
- `endpoint_contract_sample.json` captures the validator-safe endpoints plus a representative `/episodes` HTTP flow.
- the grader is now fully structured; free-text reasoning is retained for transcript clarity but does not affect score.

## Policy Benchmark Snapshot

- shallow baseline: easy=0.9567, medium=0.8460, hard=0.8217, expert=0.6967, recovery=0.9109, silent=0.8360
- cautious policy: easy=0.9567, medium=0.9300, hard=0.9350, expert=0.9550, recovery=0.9592, silent=0.9500
- aggressive policy: easy=0.7450, medium=0.6567, hard=0.6567, expert=0.7075, recovery=0.7862, silent=0.6833
- seed-sweep aggregate hard ordering: cautious_policy, shallow_baseline, aggressive_policy
- seed-sweep aggregate expert ordering: cautious_policy, shallow_baseline, aggressive_policy
- seed-sweep aggregate recovery ordering: cautious_policy, shallow_baseline, aggressive_policy
- seed-sweep aggregate silent ordering: cautious_policy, shallow_baseline, aggressive_policy
- validation summary: structural_passed=True live_passed=True

## Hard-Task Contrast

- cautious hard score=0.9350 with rollback timing=timely
- aggressive hard example score=0.8217 with rollback timing=late
- watch-window hold grader=correct_warning_hold score=0.8300
